node_modules/
test-results/
playwright-report/
.playwright/

# Next.js build output
.next/
out/

# Secrets — never commit
.env

# Data files — written only by Vercel via GitHub API, never from local machine
data/*.json
